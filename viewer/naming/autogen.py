"""AutoGen translation rules. Empty - populate when adopting this framework."""
from ._base import NamingRule

RULES = [
    # NamingRule(re.compile(r"^GroupChatManager\..*"), "Group chat"),
    # NamingRule(re.compile(r"^AssistantAgent\..*"), "Agent step"),
]
