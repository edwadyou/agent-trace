"""Smolagents translation rules. Empty - populate when adopting this framework."""
from ._base import NamingRule

RULES = [
    # NamingRule(re.compile(r"^MultiStepAgent\..*"), "Agent step"),
    # NamingRule(re.compile(r"^PlanningStep"), "Planning step"),
]
