"""CrewAI translation rules. Empty - populate when adopting this framework."""
from ._base import NamingRule

RULES = [
    # NamingRule(re.compile(r"^Crew\.kickoff$"), "Crew run"),
    # NamingRule(re.compile(r"^CrewAgent\..*"), "Crew agent step"),
]
