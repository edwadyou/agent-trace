"""LlamaIndex translation rules. Empty - populate when adopting this framework."""
from ._base import NamingRule

RULES = [
    # NamingRule(re.compile(r"^AgentWorkflow\..*"), "Agent step"),
    # NamingRule(re.compile(r"^SubQuestionQueryEngine\..*"), "Sub-question"),
]
