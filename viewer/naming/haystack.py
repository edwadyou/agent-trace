"""Haystack translation rules. Empty - populate when adopting this framework."""
from ._base import NamingRule

RULES = [
    # NamingRule(re.compile(r"^Pipeline\..*"), "Haystack pipeline"),
    # NamingRule(re.compile(r"^PromptNode\..*"), "Prompt node"),
]
