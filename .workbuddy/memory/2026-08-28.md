# 2026-08-28

- 通读了 agent-monitor 全项目（重点 viewer/ 包），未改任何代码。
- 项目结构：`agent_monitor/` 是 SDK（monitor 上下文管理器、JsonlFileExporter、auto-detect、schema 迁移、verify、CLI）；`viewer.py` 是 1680 行的 Streamlit 三栏 UI；`viewer/` 包是 viewer 的辅助层（canonical 规范 schema / normalize 字段归一化 / visibility 过滤 / naming 按框架命名规则，naming 下 8 个框架文件中仅 langchain.py 有实质规则，其余为空占位）。
- 数据流：OTel SDK -> JsonlFileExporter -> latest_traces.jsonl -> viewer.py（3 秒自动刷新）。
- 用户当前是纯阅读/熟悉代码阶段，后续可能要改 viewer。
